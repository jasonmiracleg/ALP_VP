package com.example.alp_vp.model

data class GroupUIState(
    val groups: MutableList<Group> = mutableListOf(
        Group("Team Kerja VisProg", "Membuat aplikasi android management waktu")
    )
)
